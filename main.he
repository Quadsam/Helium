#include "lib/std.he"

fn main()
{
	int statbuf[20];

	int result = stat("main.he", &statbuf);

	if result < 0 {
		print("Error: Could not stat file.\n");
		exit(1);
	}

	int size = statbuf[6];

	print("File size: ");
	print_int(size);
	print(" bytes\n");

	exit(0);
}